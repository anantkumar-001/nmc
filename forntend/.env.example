VITE_API_URL=http://localhost:3001
VITE_ESCROW_WALLET=YourSolanaEscrowWalletPublicKey
VITE_SOLANA_RPC=https://api.devnet.solana.com
# devnet | mainnet — must match Phantom’s cluster. Custom devnet RPCs without "devnet" in the URL need devnet here so Wallet Standard maps the chain correctly.
VITE_SOLANA_NETWORK=devnet
